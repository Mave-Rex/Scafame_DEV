import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: true,
  template: `
    <div class="h-10 bg-black w-full flex items-center justify-center">
    </div>
  `
})
export class HeaderComponent {}
